{
  "description": "Regenerate a Puppet module's REFERENCE.md (bundle install + bundle exec rake strings:generate:reference). Reports changed: true when REFERENCE.md differs from git HEAD; committing belongs to the git_commit_changes stage.",
  "input_method": "stdin",
  "parameters": {
    "repo_path": {
      "description": "Path to the module repository",
      "type": "String[1]"
    },
    "bundle_path": {
      "description": "Bundle install path, shared across repos for speed (default: <repo>/../../.vendor/bundle)",
      "type": "Optional[String[1]]"
    }
  }
}
