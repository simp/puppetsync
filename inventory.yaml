---
version: 2
groups:
  - name: repo_targets
    targets: []
    config:
      transport: local
      local:
        tmpdir:
          _plugin: env_var
          var: PWD
    vars:
      puppetsync:
        - _plugin: yaml
          filepath: puppetsync_planconfig.yaml
