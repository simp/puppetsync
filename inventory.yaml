---
version: 2

config:
  transport: local
  local:
    tmpdir:
     _plugin: env_var
     var: PWD

groups:
  - name: repo_targets
    targets: []
    vars:
      puppetsync:
        - _plugin: yaml
          filepath: puppetsync_planconfig.yaml
